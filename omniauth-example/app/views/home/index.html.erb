<h1>Hello</h1>

<% if session[:uid] %>
  <div>
    こんにちは <%= session[:user_name] %> さん
  </div>
  <%= button_to 'ログアウト', '/session', method: :delete, data: { turbo: false } %>
<% else %>
  <%= button_to 'Googleでログイン', '/auth/google_oauth2', data: { turbo: false } %>
<% end %>
